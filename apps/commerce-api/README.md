# @876/commerce-api

The future Commerce service currently exposes health probes only. Persistence arrives with the first real model; this service intentionally has no database configuration, Prisma schema, or migrations.
