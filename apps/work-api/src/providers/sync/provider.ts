export type WorkRemoteChange={remoteId:string;etag?:string|null;deleted?:boolean;payload:unknown};export type WorkPushResult={remoteId:string;etag?:string|null};export interface WorkSyncProviderAdapter{readonly provider:'GOOGLE'|'MICROSOFT'|'CALDAV';pull(cursor?:string|null):Promise<{changes:WorkRemoteChange[];cursor:string|null}>;push(resource:{type:'TASK'|'TASK_LIST'|'CALENDAR'|'EVENT';localId:string;remoteId?:string|null;payload:unknown}):Promise<WorkPushResult>;remove(resource:{type:'TASK'|'TASK_LIST'|'CALENDAR'|'EVENT';remoteId:string}):Promise<void>}
