/**
 * The category options the support widget offers.
 *
 * Plain data with no icons or functions, because the shell resolves them on
 * the server and hands them to a client component.
 */
export type SupportCategory = {
  id: string
  name: string
}
