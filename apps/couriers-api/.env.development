# Local development defaults for @876/couriers-api (non-secret).
# Secrets (DATABASE_URL, DIRECT_DATABASE_URL, API_876_KEY, ...) go in .env — see .env.example.
PORT=4001
ENVIRONMENT=development
LOG_LEVEL=info
CORS_ALLOWED_ORIGINS=http://localhost:3003