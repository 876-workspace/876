/* !!! This is code generated by Prisma. Do not edit directly. !!! */
/* eslint-disable */
// biome-ignore-all lint: generated file
// @ts-nocheck
/*
 * This file should be your main import to use Prisma-related types and utilities in a browser.
 * Use it to get access to models, enums, and input types.
 *
 * This file does not contain a `PrismaClient` class, nor several other helpers that are intended as server-side only.
 * See `client.ts` for the standard, server-side entry point.
 *
 * 🟢 You can import this file directly.
 */

import * as Prisma from './internal/prismaNamespaceBrowser.ts'
export { Prisma }
export * as $Enums from './enums.ts'
export * from './enums.ts'
/**
 * Model CustomerAddress
 * A customer-defined delivery destination (e.g. "Home", "Office").
 * Platform 876 billing/shipping addresses are NOT duplicated here — they live
 * in the 876 identity API and are resolved via $876 at read time.
 */
export type CustomerAddress = Prisma.CustomerAddressModel
/**
 * Model CashSession
 * A staff member's shift-based cash collection session.
 * This is a Couriers operational record. Deposit accounts, payments, balances,
 * and ledger entries stay in the shared Billing finance workspace.
 *
 * A staff member may only have one OPEN session at a time.
 * Enforced at the application level — Prisma does not support partial unique indexes.
 */
export type CashSession = Prisma.CashSessionModel
/**
 * Model CashSessionPayment
 * Operational association between a cash shift and its shared Billing payment.
 * No amount, status, payment method, or ledger data is duplicated here.
 */
export type CashSessionPayment = Prisma.CashSessionPaymentModel
/**
 * Model Branch
 * A tenant-owned Jamaica office / customer pickup location.
 * Customers are assigned a home branch at enrollment; packages are routed there by default.
 * isDefault marks the first/primary branch shown during onboarding and signup flows.
 * settings is a schemaless JSONB bag for future branch-level config (e.g. delivery options,
 * pickup hours) — structure is defined by the application layer, not enforced here.
 */
export type Branch = Prisma.BranchModel
/**
 * Model Carrier
 * Global registry of shipping carriers (e.g. UPS, USPS, FedEx, DHL).
 * Platform-wide seed data — not tenant-scoped.
 * trackingUrlTemplate uses {tracking_number} as the placeholder,
 * e.g. "https://www.ups.com/track?tracknum={tracking_number}".
 * The application substitutes the package's trackingNum to produce a clickable link.
 */
export type Carrier = Prisma.CarrierModel
/**
 * Model Contact
 * A person authorized to collect packages on behalf of a customer.
 * Either linked to an existing 876 account (userId set) or identified by
 * name + phone only. Staff verify identity at pickup against this record.
 */
export type Contact = Prisma.ContactModel
/**
 * Model CustomerIdType
 * Reference table for identity document types accepted by tenants.
 * System types (tenantId = null) are seeded globally: TRN, Passport, Driver's License, etc.
 * Tenants can add custom types (e.g. "Work Permit", "Nexus Card") by setting tenantId.
 * This data is courier-local — it serves customs clearance and package-collection KYC
 * workflows, not platform-level authentication.
 */
export type CustomerIdType = Prisma.CustomerIdTypeModel
/**
 * Model CustomerDocument
 * An identity document on file for a customer.
 * Stores the document number, an optional file upload (scan/photo), and verification state.
 * A customer can have multiple documents of different types (e.g. TRN + Passport).
 * fileUrl is a storage-layer reference resolved to a signed URL at read time.
 * verifiedById is an opaque 876 user ID — the staff member who verified the document.
 */
export type CustomerDocument = Prisma.CustomerDocumentModel
/**
 * Model CourierCustomerProfile
 * A courier-context customer: a 876 consumer user enrolled in a tenant.
 * userId references a core 876 user — opaque ID, no cross-DB FK.
 * branchId is the customer's assigned home branch — set at enrollment and used as the
 * default routing destination for all incoming packages.
 * trn is the Jamaica Taxpayer Registration Number — required by JCA for commercial
 * customs clearance on high-value imports. Stored here as courier-operational data,
 * not in the 876 identity API.
 */
export type CourierCustomerProfile = Prisma.CourierCustomerProfileModel
/**
 * Model Mailbox
 * A mailbox number assigned to a customer within a tenant.
 * Most organizations assign one per customer; the isPrimary flag marks the default.
 * number is alphanumeric and unique per tenant (used as address identifier at a Warehouse).
 */
export type Mailbox = Prisma.MailboxModel
/**
 * Model Manifest
 * A shipping manifest grouping packages for a single consolidated US-to-Jamaica shipment.
 * Created after packages are received at the warehouse and ready for consolidation.
 * Once SEALED, the package list is locked — the manifest is presented to Jamaica Customs
 * (JCA) via ASYCUDA World on arrival and the contents cannot change after that point.
 *
 * Air shipments use MAWB/HAWB numbers; sea shipments use a Bill of Lading.
 * Both modes use the same model — mode-specific fields are nullable and only populated
 * for the relevant mode.
 */
export type Manifest = Prisma.ManifestModel
/**
 * Model PackageCategory
 * Classification categories for packages — used for filtering, reporting, and display.
 * System categories have tenantId = null and are available to all tenants.
 * Tenant-custom categories are scoped to their tenantId and override or extend the system list.
 * Examples: Electronics, Apparel, Supplements, Food Items, Mail, Fitness, Footwear.
 */
export type PackageCategory = Prisma.PackageCategoryModel
/**
 * Model Package
 * A package tracked by a tenant on behalf of a customer.
 * Lifecycle: PRE_ALERT (customer-submitted) → RECEIVED → IN_TRANSIT → ARRIVED → READY_FOR_PICKUP → COLLECTED / UNCLAIMED.
 * trackingNum is nullable — pre-alerts may be submitted before the carrier has assigned one.
 * A partial unique constraint (tenant_id, tracking_num WHERE tracking_num IS NOT NULL) is enforced at the DB level via migration.
 */
export type Package = Prisma.PackageModel
/**
 * Model PackageDocument
 * A file or image attached to a package (e.g. customer receipt, seller invoice).
 * url is a storage-layer reference; the application resolves it to a signed/public URL.
 */
export type PackageDocument = Prisma.PackageDocumentModel
/**
 * Model PackageNote
 * An internal staff note on a package.
 * authorId is an opaque 876 user ID — resolved through $876 at read time.
 */
export type PackageNote = Prisma.PackageNoteModel
/**
 * Model Domain
 *
 */
export type Domain = Prisma.DomainModel
/**
 * Model Seller
 * Global registry of online retailers and sellers (e.g. Amazon, Best Buy, eBay).
 * Platform-wide seed data — not tenant-scoped. Customers link packages to a seller
 * when submitting pre-alerts so staff and customers can identify the source.
 * logoUrl is a CDN/storage reference; websiteUrl is the canonical storefront URL.
 */
export type Seller = Prisma.SellerModel
/**
 * Model StaffPosition
 * A staff position title within a tenant (e.g. "Branch Manager", "Delivery Driver", "Customer Service").
 * Positions are tenant-scoped — each organization defines their own operational hierarchy.
 * These are courier-app roles, distinct from 876 platform org roles (which govern platform access).
 */
export type StaffPosition = Prisma.StaffPositionModel
/**
 * Model StaffMember
 * A staff member employed by a tenant, optionally assigned to a branch and position.
 * userId is an opaque 876 user ID — identity details (name, email) resolved through $876 at read time.
 * A staff member may be unassigned to a branch (e.g. a head-office employee).
 */
export type StaffMember = Prisma.StaffMemberModel
/**
 * Model Tenant
 *
 */
export type Tenant = Prisma.TenantModel
/**
 * Model Warehouse
 * A US receiving address owned by a tenant.
 * Packages sent by customers are delivered here using the customer's mailbox number.
 * A tenant may have multiple warehouses (e.g. a dedicated Amazon address).
 */
export type Warehouse = Prisma.WarehouseModel
