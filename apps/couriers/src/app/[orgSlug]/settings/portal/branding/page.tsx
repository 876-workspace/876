import { Page, PageHeader, PageTitle } from '@876/ui/page'

export const metadata = { title: 'Portal branding — Settings' }

export default async function PortalBrandingSettingsPage() {
  return (
    <Page>
      <PageHeader className="mb-8">
        <PageTitle>Portal branding</PageTitle>
      </PageHeader>

      <div className="876-empty-dashed max-w-2xl">Coming soon.</div>
    </Page>
  )
}
