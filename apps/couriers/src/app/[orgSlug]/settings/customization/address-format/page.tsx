import { Page, PageHeader, PageTitle } from '@876/ui/page'

export const metadata = { title: 'Address format — Settings' }

export default async function AddressFormatSettingsPage() {
  return (
    <Page>
      <PageHeader className="mb-8">
        <PageTitle>Address format</PageTitle>
      </PageHeader>

      <div className="876-empty-dashed max-w-2xl">Coming soon.</div>
    </Page>
  )
}
