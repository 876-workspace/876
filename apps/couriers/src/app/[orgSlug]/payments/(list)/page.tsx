/**
 * The list-only state. The toolbar and the payment list live in the layout,
 * so this route renders nothing — an empty card slot is what collapses the
 * detail column.
 */
export default function PaymentsPage() {
  return null
}
