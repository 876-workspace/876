/**
 * The list-only detail state. The payment record renders as the canonical
 * Billing payment card from the layout above, which owns both the header and
 * the body — so this route renders nothing.
 */
export default function PaymentDetailPage() {
  return null
}
