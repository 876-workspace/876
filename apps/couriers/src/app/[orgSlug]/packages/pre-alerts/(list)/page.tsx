/**
 * The list-only state. The toolbar and the pre-alert list live in the layout,
 * so this route renders nothing — an empty card slot is what collapses the
 * detail column.
 */
export default function PreAlertsPage() {
  return null
}
