/**
 * List-only package state. The packages layout owns the toolbar and list so
 * they remain mounted while a package opens in the detail column.
 */
export default function PackagesPage() {
  return null
}
