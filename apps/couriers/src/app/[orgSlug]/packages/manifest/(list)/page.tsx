/**
 * The list-only state. The toolbar and the manifest list live in the layout,
 * so this route renders nothing — an empty card slot is what collapses the
 * detail column.
 */
export default function ManifestsPage() {
  return null
}
