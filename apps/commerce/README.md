# @876/commerce-app

876 Commerce is the organization workspace for a merchant's own store. This base contains access, onboarding, and shell wiring only; storefront and checkout features are deliberately absent.
