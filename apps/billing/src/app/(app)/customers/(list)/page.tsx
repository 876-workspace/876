export const metadata = {
  title: 'Customers',
  description: 'Customers in the billing workspace.',
}

/** The persistent section layout owns the customers list and toolbar. */
export default function CustomersPage() {
  return null
}
