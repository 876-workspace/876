export const metadata = {
  title: 'Items',
  description: 'Catalog items and services.',
}

/** The persistent section layout owns the items list and toolbar. */
export default function ItemsPage() {
  return null
}
