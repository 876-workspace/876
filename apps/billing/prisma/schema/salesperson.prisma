/// Optional commercial owner used for attribution and future commission
/// reporting. It does not grant Billing permissions.
model Salesperson {
  id                String  @id
  tenantId          String  @map("tenant_id")
  name              String  @db.VarChar(160)
  email             String?
  externalReference String? @map("external_reference")
  isActive          Boolean @default(true) @map("is_active")
  createdAt         Int     @map("created_at")
  updatedAt         Int     @map("updated_at")

  tenant    Tenant     @relation(fields: [tenantId], references: [id], onDelete: Cascade, onUpdate: Cascade, map: "billing_salespeople_tenant_fkey")
  customers Customer[]
  invoices  Invoice[]

  @@unique([tenantId, id], map: "billing_salespeople_tenant_id_id_key")
  @@unique([tenantId, externalReference], map: "billing_salespeople_tenant_external_reference_key")
  @@index([tenantId, isActive], map: "billing_salespeople_tenant_active_idx")
  @@map("billing_salespeople")
}
