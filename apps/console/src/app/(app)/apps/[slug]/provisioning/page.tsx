export const metadata = { title: 'Provisioning profiles' }

/** The persistent App Provisioning layout renders the toolbar and profiles table. */
export default function AppProvisioningPage() {
  return null
}
