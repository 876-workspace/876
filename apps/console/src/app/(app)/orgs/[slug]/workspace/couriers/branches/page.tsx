import { createCouriersBranchesPage } from '../../_components/couriers-workspace-pages'

const { Page, generateMetadata } = createCouriersBranchesPage()

export { generateMetadata }
export default Page
