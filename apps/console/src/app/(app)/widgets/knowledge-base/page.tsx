import { knowledgeBaseWidgetMetadata } from '@876/widgets'

import { WidgetOverview } from '@/components/widgets/widget-overview'

export default function KnowledgeBaseWidgetPage() {
  return <WidgetOverview widget={knowledgeBaseWidgetMetadata} />
}
