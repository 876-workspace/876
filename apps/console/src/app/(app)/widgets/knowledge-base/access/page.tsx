import { knowledgeBaseWidgetMetadata } from '@876/widgets'

import { WidgetAccessList } from '@/components/widgets/widget-access-list'

export default function KnowledgeBaseWidgetAccessPage() {
  return <WidgetAccessList widget={knowledgeBaseWidgetMetadata} />
}
