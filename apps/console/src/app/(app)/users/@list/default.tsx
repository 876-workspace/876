import { UsersListData } from '../_components/users-list-data'

/** Fallback for a hard navigation whose slot state cannot be recovered. */
export default function UsersListDefault() {
  return <UsersListData />
}
