export const metadata = { title: 'Provisioning setups' }

/** The persistent Provisioning layout renders the toolbar and setup table. */
export default function ProvisioningSetupsPage() {
  return null
}
