import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Customers - Requests' }

/** The persistent customers layout owns the toolbar and list column. */
export default function PlatformRequestCustomersPage() {
  return null
}
