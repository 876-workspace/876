import { PLATFORM_REQUESTS_HREF } from '@/features/crm/request-paths'

/** 876's own service-desk customers, inside the Requests section. */
export const PLATFORM_CUSTOMERS_HREF = `${PLATFORM_REQUESTS_HREF}/customers`
