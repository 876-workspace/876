# AI attribution policy

Git authorship and co-authorship in this repository identify human contributors only.

Historical AI identities that leaked into commit metadata are canonicalized through the root `.mailmap`. New AI attribution is rejected by the repository commit-message guard and CI policy check.

The retired `.grok/` workspace must not be recreated. Supported agent rule trees are `.claude/rules/` and `.agents/rules/`.
