# Brief C — Couriers packages onto the shared split view

Repo /root/projects/876, branch feature/couriers-modernization. Do NOT commit, branch, push, or write log files. The reference implementation to copy EXACTLY is apps/couriers/src/app/[orgSlug]/customers/ in this same app (layout.tsx renders a *-section.tsx through @876/ui/list-detail-section; *-list.tsx renders full table when closed and condensed ListPane when open via useDetailSegments; *-list-data.tsx server data; (list)/page.tsx returns null; [id]/(detail)/layout.tsx + page.tsx render the DetailCard; layout awaits params only; notFound() inside the streamed server component). Also Billing reference: apps/billing/src/app/(app)/items/.
Read first: .claude/rules/app-layout.md §5a (Height checklist), navigation-performance.md, data-loading.md, testing.md, error-handling.md, ai-code-quality.md, app-structure.md.
Rules: toolbar + StatusFilterHeading stays mounted; condensed row keeps status badge; no eslint-disable / as any / @ts-ignore; do not weaken production code for tests; expected errors are values rendered with AppError, not throws. Data goes through the host modules under apps/couriers/src/lib/services/ (grep existing usage first); never raw fetch. If the backend lacks a retrieve-one capability, STOP and say so in the report rather than inventing one.
Verify in the foreground from apps/couriers: npx tsc --noEmit; npx eslint src; npx vitest run; then from repo root node scripts/check-app-structure.mjs. Delete stale .next/types if tsc complains about moved routes.

Goal: apps/couriers/src/app/[orgSlug]/packages/ (the main list page only) becomes a real data-backed list (currently a static placeholder) that opens a package beside the list at /packages/[id], using the couriers packages capability (packages/couriers/src + apps/couriers-api/src/modules/packages). Keep the existing status filter values. Detail card: package identity, status, customer/mailbox, weight/dimensions and dates that the API actually returns — no invented fields.
Scope: apps/couriers/src/app/[orgSlug]/packages/{layout.tsx,page.tsx,_components/**} plus new (list)/ and [id]/ dirs. DO NOT touch packages/warehouse, packages/manifest, packages/pre-alerts, packages/deliveries, or items/.
Minimum tests: 10 it() cases (list open/closed, selected row, status filter, data error notice, detail notFound, detail render).
Report: plans/2026-09-13-couriers-modernization/reports/opencode/packages-split-view.md (files, test count, verification output, gaps).
