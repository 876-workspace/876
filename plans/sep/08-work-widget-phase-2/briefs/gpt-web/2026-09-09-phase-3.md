# GPT Web Brief — Recover and Complete Work Widget Phase 3

## Start condition and branch

Do not begin until the focused Phase 2 PR has merged to `main`.

1. Fetch and branch `feat/work-widget-phase-3` from updated `main`.
2. Recover the retained Phase 3 implementation from
   `origin/feat/work-widget-phase-2@c1f43471f`.
3. Replay only Phase 3 application/test changes. Do not copy the mixed Phase 2
   tracker/report commits, the abandoned task-delete commits (`4d45b8264`,
   `64ed4d5fe`), or their scope-revert commits (`6f1b8c70b`, `d2f6dfc62`).
4. Preserve the focused Phase 2 fixes and resolve replay conflicts in their
   favor, especially the exclusive Today time boundary and catalog typing.

## Required stabilization before new scope

The retained code is prework, not a green implementation. Stabilize it before
adding Phase 3D or later features.

- Fix `packages/work-ui/src/agenda.tsx` without `any`, unsafe casts, or relaxed
  compiler settings. Build explicitly typed agenda arrays instead of relying on
  `flatMap` inference across incompatible all-day/timed event literals.
- Update the four browser mutation tests to account for the canonical
  `requestApiResult` transport adding a `Headers` object. Assert the meaningful
  method/body/header contract; do not change shared transport to satisfy stale
  mocks.
- Run typecheck/tests immediately after replay. Do not continue while the
  recovered baseline is red.

## Authorization and capability UX

Server authorization already remains mandatory. Also make the reusable UI
capability-aware:

- A viewer with `my-work.view` and read-only task/calendar permissions must not
  see create, edit, complete, cancel, or delete controls.
- Pass narrow booleans derived from the effective Invoice permissions into the
  widget renderer/UI. Do not fetch a second permission model from the browser,
  trust client state as authorization, or expose a server credential.
- Keep route checks for `tasks.view`, `tasks.create`, `tasks.edit`, and future
  destructive actions even when the corresponding control is hidden.
- Add tests for read-only Staff, non-destructive Admin, and Super Admin control
  visibility plus server-side denial.

## Contract and route rules

- Keep browser paths in Invoice vocabulary: `/api/my-work`, `/api/tasks`,
  `/api/task-lists`, and `/api/calendars`. Do not expose Work service origins,
  `/v1`, app keys, or internal keys.
- Keep `@876/work-ui` controlled and transport-agnostic. Network orchestration
  stays in the widget/host layer.
- Host request schemas may intentionally be narrower than Work create/update
  schemas, but after injecting acting-user fields, validate the transformed
  payload with canonical `@876/work` schemas before calling the client. Do not
  maintain a second independent definition of Work invariants.
- Expected failures remain `{ data, error }` values and render locally.

## Data loading and resilience

- Add real cursor-based load-more behavior for tasks. Do not ship the placeholder
  text that says pagination will be added later.
- Treat task lists and calendar lists as secondary enrichment where possible. A
  failed list request must not discard a successful task or My Work result.
- Keep stale successful data mounted during refresh errors and distinguish an
  unavailable dataset from a genuinely empty dataset.
- Guard rapid duplicate mutations with request state that is effective before a
  rerender, not only a state value captured by the prior render.

## Calendar correctness

- Add pure tests for day/week/month windows, month spillover, exclusive range
  ends, daylight-saving transitions, timed cross-midnight events, all-day
  `[startDate, endDate)` semantics, and selected-calendar filtering.
- Calendar selection filters only events owned by that calendar; it must not hide
  tasks or reminders.
- Review the 520px widget experience. The retained 560px month grid and 720px
  week grid fall back to horizontal scrolling; provide the planned compact
  month/selected-day and medium layouts rather than treating overflow as the
  responsive design.

## Maintainability gate

The retained `WorkWidgetPanel` reached roughly 500 lines and owns Today, Tasks,
Calendar, three loading pipelines, mutation refresh, and navigation state. Do not
grow it further. Separate focused per-view orchestration while keeping shared UI
components presentation-only; prefer deleting coupled state and branches over
adding generic wrappers.

Run the thermo-nuclear code-quality review before requesting a commit. The Phase
3 branch is not ready until all package/app typechecks and tests, Invoice build,
format/lint, transpile/service-bundle checks, mergeability, CI, and automated
review threads are green or explicitly and correctly classified.
