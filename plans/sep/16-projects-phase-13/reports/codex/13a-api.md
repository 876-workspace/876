# Report 13a — Projects API workflow automation (transitions, blueprints, rules, outbox worker)

## Scope
- Touched only `apps/projects-api/**` and `packages/projects/**`. No commit/branch/push. No `prisma migrate` (hand-written SQL only; `prisma validate` + `prisma generate` for verification). No `eslint-disable` / `as any` / `@ts-ignore` (verified via grep). No run logs left in the tree.
- Note: `git status` also shows `packages/projects-ui/**` paths from the parallel 13b UI track; none of those are mine.
- This session resumed mid-brief: all source was written by a prior pass but the suite had never been run. Verification exposed real breakage (below); all fixed, full suite green.

## Delivered (`apps/projects-api`)
- Migration `prisma/migrations/20260925000000_workflow_automation/migration.sql` (hand-written) + `prisma/schema/workflow-automation.prisma`: `WorkflowTransition` (type/blueprint rows: from/to state keys, required field keys, required permission, requires-comment), `AutomationRule` (trigger, conditions/actions JSON, sealed `webhookSecret` JSON, soft-delete `deletedAt`), `AutomationEvent` outbox (`tenantId`, `type`, `subjectType/Id`, `payload` JSON, `causationDepth`, `createdAt/claimedAt/processedAt`, `attempts`, `nextAttemptAt`), `AutomationRun` (unique `(ruleId,eventId)`, `responseCode`), `Notification` (`tenantId`, `userId`, `kind`, `title`, `subjectType/Id`, `readAt`). Tenant back-relations in `tenant.prisma`. IDs `wft_/arl_/aev_/arn_/ntf_` in `platform/ids.ts`; error codes `transition-not-allowed` (422), `transition-requirements-unmet` (400), `automation-rule-not-found`, `automation-subject-not-found`, `notification-not-found`.
- `src/platform/secure-field.ts`: binds `@876/core/crypto/secure-field` (local AES key via `SECURE_FIELD_KEY`), context `{tenant_id, automation_rule_id, type:'webhook-secret'}`. Secrets sealed on rule write; reads expose only `hasWebhookSecret`, never the secret or ciphertext.
- `src/modules/workflows/`: blueprint `GET/PUT /workflows/:workItemTypeId/blueprint` (rejects unknown state keys and duplicate from/to pairs); `checkTransition()` — empty transition set allows everything (backwards compatible), null-from matches any state, aggregates missing permission/fields/comment into one `transition-requirements-unmet` error.
- Transition enforcement in `issues.service` update path: resolves layout values for required-field checks, forwards optional `comment` (new optional `UpdateIssueBody.comment`) and caller permissions parsed by the controller from `x-actor-permissions`.
- `src/modules/automation/`: rules CRUD (`GET/POST /automation-rules`, `GET/PATCH/DELETE /automation-rules/:id`, soft delete), `GET :id/runs`, `POST :id/test` dry-run (per-condition match flags + planned actions, zero side effects); notifications `GET /notifications?userId`, `POST /notifications/:id/read`; `sweepAutomationEvents` (due-approaching 72h window via `issues.listDueSoon`, budget-threshold via `finance.getFinancialSummary`, per-day-per-subject dedupe via `existsEventSince`); `webhook.ts` (`t=<ts>,v1=<hmac-sha256>` over `"${ts}.${body}"`, 10s `AbortController` timeout, secret never logged).
- Outbox append in the **same transaction** as the mutation via `automation.appendOutboxEvent(txClient, …)` (zero-dep leaf over the passed tx client): issue create (`work-item.created`), issue update (`work-item.updated` + `work-item.state-changed`), time-entry submit (new `time.repository.transaction`, exposes `client`), milestone completion (new `work-structure.repository.transaction`; `core.updateMilestone(..., {client})` writes through `client.milestone.update` when a client is passed). Absent on rollback (tested).
- `src/workers/automation.ts`: `drainAutomation` (sweep → claim → process); claim SQL with `FOR UPDATE SKIP LOCKED` via `$queryRaw` in the repository (`buildClaimEventsQuery`, camelCase aliases); depth guard `causationDepth > 3`; idempotency via succeeded-run check per (rule,event); backoff `30·2^(n-1)` capped 3600s, max 5 attempts then park; all 9 action types call owning-module public services only (issues/labels/calendar/notifications), actor `automation:<ruleId>`, child depth parent+1, automation passes empty permissions (fail-closed on permission-gated transitions). Thrown action errors are caught per rule (`automation/action-failed`) so a DB outage retries instead of crashing the drain. `POST+GET /internal/automation/drain` behind `requireInternalKey` (GET exists because Vercel cron invokes GET); bounded limit, returns counts. `vercel.json` cron `*/10 * * * *` added.
- `vitest.config.ts`: `env.PROJECTS_DATABASE_URL` dummy on a closed port, mirroring `apps/api` / `apps/billing-api` precedent (repositories construct the Pool at import time; tests mock every repository so nothing is dialed).

## Delivered (`packages/projects`)
- `matchesConditions` exported from `src/layout-rules.ts` (existing `ruleApplies` refactored onto it — single matcher). New `automation-types.ts` content in `types.ts`, resources `workflows.ts`, `automation-rules.ts`, `notifications.ts` wired into `client.ts` + `index.ts`.

## Fixes found during verification (prior pass never ran the suite)
- `src/workers/automation.ts` imported `layout-rules` with three `../` instead of four — every worker/route test failed to load. Fixed (module files correctly use five, matching the `layouts.service.ts` comment convention).
- Unconfigured suite: 17 files failed collection on `PROJECTS_DATABASE_URL`. Fixed via vitest `env` (sibling-service precedent), not per-file mocks.
- 4 pre-existing files (`issues`, `issue-links`, `time`, `work-structure.routes` tests) executed the real automation/workflows code: added `automation`/`workflows` module mocks (same pattern as the in-file Phase-2 comment precedent), `transaction` to the `time`/`work-structure` repository mocks, and a `client.milestone.update` shape to the work-structure tx stub.
- Implementation bugs fixed: `readRuleSecret` lived in `automation.service` but the worker imported it from serializers (moved to serializers); worker did not catch thrown action errors (added try/catch → retry path); `webhookSecret: null` rejected by Prisma 7 JSON types (repository maps null → `Prisma.DbNull`).
- Test bugs fixed (all in new tests): wrong `secure-field` mock path, outbox tests asserting on the tx client instead of the repository mock, missing `resolveWorkItemTypeById` mock, `resolveTenant` shape + generated-id spread order in time/issue mocks, constant-30 delay mock defeating the backoff-growth assertion, unhandled-rejection warning in the webhook timeout test, missing `automationRules` key in client namespace lists.
- Added explicit tenant-isolation worker test (lookup scoped to `event.tenantId`; runs/notifications recorded under the event tenant).

## Caveats
- Vercel cron cannot send `x-internal-key`; the `/internal/automation/drain` cron entry will 401 unless the platform injects the header or an allowlist for the cron caller is added — needs a follow-up decision.
- `GET /internal/automation/drain` exists solely for the cron caller; POST is the primary contract.

## Verification (each run separately, in order)
- `pnpm --filter @876/projects-api exec prisma validate` — schemas valid.
- `pnpm --filter @876/projects-api exec prisma generate` — client 7.9.1 generated.
- `pnpm --filter @876/projects-api typecheck` (`tsc --noEmit`) — clean.
- `pnpm --filter @876/projects-api lint` — clean (only the pre-existing Pages-directory notice).
- boundaries (`module-boundaries.test.ts`) — 4 passed.
- `pnpm --filter @876/projects-api test` — 46 files, **1110 passed** (108 new `it()` across 9 new/affected files: workflows.service 20, automation.service 23, automation.routes 12, worker 28, issue-transitions 10, webhook 6, outbox 5, time-outbox 2, milestone-outbox 2 — floors met: transitions allowed/denied/requirements, no-transition compat, outbox-in-tx + rollback absence, skip-locked shape, retry/backoff/max-5, (rule,event) idempotency, depth guard, all 9 actions, webhook signature bytes + timeout, secret never serialized, dry-run purity, sweep dedupe, tenant isolation).
- `pnpm --filter @876/projects typecheck` — clean.
- `pnpm --filter @876/projects test` — 36 files, **249 passed** (24 new: matches-conditions 8, automation-types 5, automation-rules resource 7, notifications 2, workflows 2).
