# Brief — finish D3: clone + make recurring (backend only)

Branch `feat/invoice-document-parity`. Six commits are clean; the working tree
holds an unfinished delegate's WIP. Read
`plans/2026-09-11-invoice-document-parity/briefs/codex/2026-09-11-phase-d-toolbar.md`
section **D3** — it governs. **Touch `apps/billing-api` and `packages/billing`
only.** Another agent is working in `packages/billing-ui` and the two Next apps
at the same time; do not edit those.

## State of the WIP, and what is broken

Uncommitted: `repositories/invoices/clone.{ts,test.ts}` (7 tests, passing),
edits to `documents.{routes,controller,service}.ts`,
`recurring-invoices.service.ts`, `repositories/recurring-invoices.repository.ts`,
`schemas/recurring-invoice.ts`, `repositories/invoices/index.ts`, and
`__tests__/recurring-invoices.repository.test.ts`.

**Three failures, all caused by that WIP** — reproduce them first:

1. `src/test/openapi-contract.test.ts` — "Frozen operations: 333; Express
   operations: 337". Four routes were added without updating the frozen
   contract. `api-backend.md` treats that contract as a **compatibility
   oracle**: regenerate/extend it deliberately and say what the four new
   operations are, do not weaken the test.
2. `src/http/auth/__tests__/full-route-auth-matrix.test.ts` — same cause; the
   new routes need their auth tier recorded in the matrix.
3. `__tests__/recurring-invoices.repository.test.ts` >
   `createRecurringInvoiceFromInvoice > stores the invoice line snapshots on the
   profile` — a real bug: it asserts `rinvl_test_1` and gets `rinvl_test_2`.

Judge the WIP before extending it. If an implementation is wrong, fix or delete
it and say so — do not build on something broken.

## Finish

- **Clone** — a DRAFT invoice from an existing one: same customer, currency,
  lines, terms, notes, discount; fresh number and dates; no payments or credits
  carried over. Route it, and expose it in `packages/billing`.
- **Make recurring** — a profile from an invoice. `plan.md` records the exact
  field mapping; `lines` is the same `DocumentLineCreateSchema` on both sides.
  The caller supplies `profileName`, `frequency`, `startAt`, `endAt`,
  `maxCycles`, `generationMode`. Expose it in `packages/billing` too.
- Both reuse the existing create path. No duplicated business logic.

**Test floor ≥12** across both, per the Phase D brief.

## Prohibitions and verification

No commits. No branch operations. No `eslint-disable` / `@ts-ignore` / `as any`.
No `prisma migrate`. No run logs in the repo. Do not touch `packages/billing-ui`
or either Next app. Run in the foreground, separately:

```
pnpm --filter @876/billing-api typecheck
pnpm --filter @876/billing-api test
pnpm --filter @876/billing-api boundaries
pnpm --filter @876/billing typecheck
```

Report to `plans/2026-09-11-invoice-document-parity/reports/codex/2026-09-11-d3.md`
with counted `it()` numbers, the four new operations named, and real output.
