# Brief — finish Phases B and C

Branch `feat/invoice-document-parity`, uncommitted work already in the tree.
Read, in this order:

1. `plans/2026-09-11-invoice-document-parity/briefs/codex/2026-09-11-invoice-document-parity-phases-a-c.md` — the original spec, still governing.
2. `plans/2026-09-11-invoice-document-parity/reports/codex/2026-09-11-phases-a-c.md` — the previous run's honest handoff.

Phase A is **done** (shared panel + adapter + 14 passing tests). Do not redo it.

## What is actually left

**Phase B — implemented, zero tests.** `schemas/invoice.ts` and
`repositories/invoices/update.ts` now accept a replacement `lines` array for
DRAFT only, via `buildDocumentLines` + `calculateDocumentTotals`. Write the
**≥12 test floor** from the original brief against what is there. If the
implementation is wrong, fix it — but read it before assuming it is.

**Phase C — not done, and the previous run said so.** It widened the existing
`invoice-edit-form.tsx` in both apps and bolted a `DocumentLineItemsEditor` on,
rather than converting the edit page to the create form. Finish it properly:

- Give each app's own `DocumentCreateForm` an edit mode (an `initialDocument` /
  `mode` prop plus a submit-handler prop) and render it from the edit page, at
  the same width and chrome as that app's `new/page.tsx`. This is option (a)
  from the original brief. The previous run declined a full de-fork of the two
  create forms — **that decision stands**; do not attempt it either. Each app
  keeps its own form; each gains an edit mode.
- Delete `invoice-edit-form.tsx` in both apps once nothing imports it (grep).
- Respect `getInvoiceEditability(status)`: DRAFT gets the line-items editor, a
  `restricted` invoice gets only the fields it may change and no editor.
- **≥10 tests** across both apps, per the original brief's list.

## Also fix

`apps/billing/.../invoices/[invoiceId]/page.tsx` carries
`as unknown as InvoiceDetail` because Billing's `resolveInvoice` is a legacy
passthrough record. Remove it if the typed retrieve can be used directly; if it
genuinely cannot, leave it and restate the justification in your report.

## Verification — foreground, and finish what the last run did not

```
pnpm --filter @876/billing-api test
pnpm --filter @876/billing-app lint && pnpm --filter @876/billing-app test
pnpm --filter @876/invoice-app lint && pnpm --filter @876/invoice-app test
pnpm --filter @876/billing-ui typecheck
pnpm check:transpile
node scripts/check-app-structure.mjs
```

`check-app-structure.mjs` currently fails on a **pre-existing, unrelated** Console
violation (`apps/console/src/components/shell/sidebar.tsx: ConsoleHome`). Do not
fix it, do not let it mask a violation of your own — run the commands separately
rather than chained with `&&`.

## Prohibitions and report

Same as the original brief: no commits, no branch operations, no `eslint-disable`
/ `@ts-ignore` / `as any`, no `prisma migrate`, no run logs in the repo, no
placeholder UI, and do not build Phase D or E — they are briefed separately.

Report to `plans/2026-09-11-invoice-document-parity/reports/codex/2026-09-11-finish-b-c.md`
with counted `it()` numbers per phase and the real verification output.
