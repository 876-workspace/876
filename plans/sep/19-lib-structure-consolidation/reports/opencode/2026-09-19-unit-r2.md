# Unit R2 — rename `src/lib/service/` to `src/lib/records/` (Console + Widgets API)

Date: 2026-09-19. Pure rename, no behaviour change, no compatibility alias.

## What was done

1. `git mv apps/console/src/lib/service apps/console/src/lib/records`
2. `git mv apps/widgets-api/src/lib/service apps/widgets-api/src/lib/records`
3. Renamed `export const service` → `export const records` in both moved `index.ts` files.
4. Rewrote all importing files: `@/lib/service` → `@/lib/records`,
   `@/lib/service/<x>` → `@/lib/records/<x>`, `{ service }` → `{ records }`,
   `service.<resource>` → `records.<resource>` at those call sites, and
   `vi.mock('@/lib/service', () => ({ service: … }))` →
   `vi.mock('@/lib/records', () => ({ records: … }))`.
5. Fixed relative imports in `apps/widgets-api/src/lib/errors.ts`
   (`./service/collections/types`, `./service/notes/types` → `./records/…`).
   No relative `service` imports existed in Console scope.
6. No `lib/service` strings in either app's `vitest.config.ts` / `tsconfig.json`
   (checked — none before or after).
7. No local `records` binding collision: none of the 31 edited files binds a
   `records` name (the repo's seven `const records` bindings live in unrelated
   feature files such as `custom-module-records-data.tsx`, which do not import
   the renamed module).

### Files per app

Console — 12 call-site files (`{ service }` → `{ records }` + usage):
`lib/access/team-list-data.ts`, `lib/auth/access-context.ts`,
`app/(app)/users/[username]/_data.ts`,
`app/(app)/settings/users/(team)/[id]/_data.ts`,
`app/(app)/settings/users/roles/[name]/_data.ts`,
`app/(app)/settings/users/roles/layout.tsx`, `app/api/users/route.ts`,
`app/api/users/[id]/role/route.ts`, `app/api/users/[id]/route.ts`,
`app/api/roles/[name]/route.ts`, `app/api/roles/route.ts`,
`app/api/team/[id]/route.ts`;
7 test files with `vi.mock('@/lib/service')` (path + `service:` → `records:` key);
path-only: `lib/auth/role-change.ts` (`team`, `TeamServiceResult` identifiers kept),
`lib/auth/role-change.test.ts` (`vi.mock('@/lib/service/team')` path only),
plus subpath type import in `team-list-data.ts`. 30 files moved by `git mv`.

Widgets API — 7 route files (`{ service }` → `{ records }` + usage):
`app/api/v1/admin/notes/route.ts`, `app/api/v1/admin/notes/[id]/route.ts`,
`app/api/v1/admin/stats/notepad/route.ts`, `app/api/v1/notes/route.ts`,
`app/api/v1/notes/[id]/route.ts`, `app/api/v1/collections/route.ts`,
`app/api/v1/collections/[id]/route.ts`;
1 test file with `vi.mock('@/lib/service')`;
path-only: `lib/http.ts` (`ServiceResult` type kept), `lib/errors.ts` (relative);
`parseColor` / `parseCollectionColor` identifiers kept. 20 files moved by `git mv`.

Untouched as required: `apps/billing/` (zero status entries), all `@/lib/clients`
imports (392 files reference them; none edited), `service-key` /
`serviceResponse` / `requireWidgetsService` identifiers, no alias/re-export,
no `eslint-disable` / `@ts-ignore` / `as any` added, no commit/branch/PR.

## Required greps

```
grep -rn "@/lib/service'" apps/console/src apps/widgets-api/src | wc -l  # 0
grep -rn "@/lib/service/" apps/console/src apps/widgets-api/src | wc -l  # 0
git status --short -- apps/billing | wc -l                               # 0
```

Extra: bare double-quote `@/lib/service"` → 0; `vi.mock('@/lib/service…` → 0;
relative `from '…service/…'` → 0 in both scopes.

## Verification output

- `pnpm --filter @876/console typecheck` — PASS (`tsc --noEmit`, no errors).
- `pnpm --filter @876/console lint` — 0 errors, 22 warnings; none in touched
  files (all pre-existing in files such as `route-guard.ts`, `platform-org.ts`).
- `pnpm --filter @876/console test` — 251/252 files, 2040/2044 tests pass.
  The 4 failures are all in `src/lib/permissions.test.ts` (permission-count pins),
  which contains zero `service`/`records` references and was not touched; the
  worktree already had unrelated uncommitted feature-flag edits
  (`apps/876/src/lib/features.ts`, `apps/crm/…`, etc.) before this task started.
- Targeted re-run of all 17 rename-adjacent console suites
  (`team-list-data`, `access-context*` ×5, `guards`, `role-change`,
  `team/[id]` route, everything under `lib/records/`) — 279/279 PASS.
- `pnpm --filter @876/widgets-api typecheck` — PASS.
- `pnpm --filter @876/widgets-api test` — 7/7 files, 82/82 tests PASS.
- `node scripts/check-app-structure.mjs` — `app-structure: OK (…)`.

## Anything unverified / intentionally left

- Doc-comment mentions of the old name were deliberately left untouched as
  outside the rename scope (not resolvable imports): the JSDoc usage example in
  `apps/console/src/lib/records/index.ts` (`` `service.team.list()` ``),
  `apps/console/src/lib/db/index.ts:135` ("Only `@/lib/service` may query this"),
  `apps/console/src/types/api.ts:3`, and `it(…)` prose strings in tests
  (e.g. `'delegates to service.team.retrieve …'`). Say the word if these should
  follow in a docs pass.
- The 4 `permissions.test.ts` failures were not investigated beyond confirming
  they are unrelated to this rename (untouched file, dirty tree predates task).
