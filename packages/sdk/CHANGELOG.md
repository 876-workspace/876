# Changelog

All notable changes to `auth876` will be documented in this file.

This package follows Semantic Versioning.

## 0.1.0

- Initial request-only SDK scaffold.
- Added Zod-backed public auth contracts.
- Added branded `auth876` singleton and `createAuth876Client` factory.
- Added optional app-authenticated request headers through `apiKey` and `appId` client options.
