# Brief — refresh `apps/api/docs/express-migration.md` to the current state

**Tool:** `agy --model=claude-sonnet-4-6` (do **not** pass `--effort`; the Sonnet
model rejects it and the run dies instantly)
**Type:** docs-only. You must not touch any `.ts`, `.py`, `.json`, or `.prisma` file.

> A first attempt on `gemini-3.6-flash-high` died with `Error: timeout waiting
for response` before writing anything. This document is the migration's
> handoff, so it goes to Sonnet.

## The one file you may edit

`/workspaces/876/apps/api/docs/express-migration.md`

Edit **only** that file. Do not create new files. Do not run `git commit` — the
orchestrating agent commits.

## Why this matters

That document is the handoff every future session reads first. It is currently
**stale**: it describes work as unfinished that has since landed, and it tells
the next session to review files that are already reviewed and committed. A
session that trusts it will redo work that is done. Your job is to make it
truthful again.

## Context you need

Nine commits landed on branch `feat/api-express-foundation` after the document
was written. The whole of "Batch A" except three units is now committed and
verified. Nothing is uncommitted in the tree any more.

Verification as of now, all four green:

- `pnpm node:typecheck` — clean
- `pnpm node:lint` — clean
- `pnpm node:test` — **434 passing** in **18 files** (the doc says 212)
- `pnpm node:boundaries` — **0 errors**, 13 warnings (all `*.docs.ts` files with no routes yet)
- `npx prettier --check "src/**/*.ts"` — clean

## The exact edits to make

Work through these in order. Each one names what to find and what it becomes.
Preserve the document's existing voice, heading levels, and table formatting.

### Edit 1 — Section 1, the verification block

In the fenced `bash` block under "Green as of the last run", the comment on the
`pnpm node:test` line currently reads `# 212 passing`. Change it to
`# 434 passing, 18 files`.

### Edit 2 — Section 1, the "Landed" table

Add two new rows to the bottom of the `| Area | State |` table:

| Platform primitives | `phone`, `pin` (scrypt, cross-checked against a Python-generated hash), `rate-limit`, `risk`, `user-agent`, `secure-field`; `AppHttpError` moved to `platform/errors.ts` so leaf layers can raise it |
| Providers | `providers/communications.ts` (the neutral contract), `providers/twilio/**` (client, adapter, errors, fake, webhook signatures), `providers/posthog/**` |

### Edit 3 — Section 2, replace the whole section

Section 2 is currently titled `## 2. Uncommitted work in the tree right now`
and describes an unfinished agy run. **All of it is obsolete** — that work is
finished, reviewed, and committed. Replace the entire section, heading included,
with this:

```markdown
## 2. How the last batch was verified

Batch A's platform primitives and vendor adapters are committed. Every port was
checked against its Python source before it was committed, not after — the
methods are worth reusing, because two of them found real divergences:

- **`user-agent.ts`** carries its own regex table (there is no maintained
  JavaScript binding to uap-core). It was verified by running **both**
  implementations over the same 15 user agents and diffing all 8 stored fields —
  **120 comparisons, 0 differences.** Reproduce it by dumping
  `parse_user_agent` from the venv and `parseUserAgent` from a throwaway Vitest
  file, then diffing the JSON.
- **`twilio/signatures.ts`** implements Twilio's HMAC-SHA1 scheme directly. All
  four test vectors were **regenerated with the real Twilio Python SDK's**
  `RequestValidator.compute_signature` and matched, and the live validator
  accepts each one.
- **`pin.ts`** verifies against a hash generated by `core.pin.hash_pin`, so the
  two services can read each other's stored PINs.
- **`risk.ts`** needed a fix found by reading: `Number(' ')` is `0` in
  JavaScript where Python's `float(' ')` raises, so a whitespace-only coordinate
  would have been read as the equator instead of "unknown".

The lesson to carry forward: a round-trip test proves a port is
self-consistent, not that it agrees with the service it replaces. Anything
carrying a hash, a signature, or a vendor's regex corpus needs a
cross-implementation fixture.
```

### Edit 4 — Section 5, the Batch A table

Replace the whole `| Unit | Source | Owner |` table with this. Six units are now
done; three remain.

| Unit                          | Source                                                                                                | State         |
| ----------------------------- | ----------------------------------------------------------------------------------------------------- | ------------- |
| `platform/secure-field.ts`    | `core/secure_field.py`                                                                                | **done**      |
| `platform/phone.ts`           | `core/phone.py` (45)                                                                                  | **done**      |
| `platform/pin.ts`             | `core/pin.py` (125)                                                                                   | **done**      |
| `platform/rate-limit.ts`      | `core/rate_limit.py` (87)                                                                             | **done**      |
| `platform/risk.ts`            | `core/risk.py` (148)                                                                                  | **done**      |
| `platform/user-agent.ts`      | `core/user_agent.py` (209)                                                                            | **done**      |
| `providers/communications.ts` | `providers/communications.py`                                                                         | **done**      |
| `providers/twilio/`           | `providers/twilio/*.py` (585)                                                                         | **done**      |
| `providers/posthog/`          | `providers/posthog/client.py` (185)                                                                   | **done**      |
| `platform/permissions.ts`     | `core/org_permissions.py` — a pure catalog plus the default role definitions                          | primary agent |
| `platform/session.ts`         | `core/session.py` — iron-session sealing, must stay compatible with `unsealSession876` in `@876/core` | primary agent |
| `providers/workos/`           | `providers/workos/*.py` (~900)                                                                        | primary agent |

Immediately after that table, replace the sentence beginning "Everything after
this batch depends on some part of it" with this paragraph:

```markdown
**The permissions unit was mis-scoped in an earlier draft and is now resolved.**
`core/permissions.py` does not exist. The platform primitive is
`core/org_permissions.py` — the org permission catalog and the default role
definitions, pure data with no I/O. The DB-backed `resolve_member_permissions`
lives in `services/provisioning.py` and belongs to **Batch E**, not here. Do not
try to port it as a platform primitive; it needs a session.

The three remaining Batch A units are all auth- or credential-critical, so per
`.claude/rules/cli.md` they stay with the primary agent and are not delegated.
```

### Edit 5 — Section 5, Batch D row

In the Batches B–H table, the **D / directory** row's "Suggested owner" cell
currently reads `agy or Codex — 154 doc constants of repetitive CRUD`. Change
that cell to:

`agy (delegated 2026-08-07) — 50 routes, 10 resources x 5 verbs`

### Edit 6 — Section 5, "Tool state"

In the **Codex** bullet, the sentence says it "hit its usage limit 2026-08-06,
resets **2026-08-08 04:00Z**". Leave the dates exactly as they are.

In the **agy** bullet, keep the existing warnings and **append** this sentence to
the end of that same bullet:

`Scoping one unit per invocation fixed the timeout — the four single-unit briefs on 2026-08-07 all completed.`

### Edit 7 — Section 6, add one trap

Add these two bullets to the end of the "Traps already hit" list:

```markdown
- **`Number(' ')` is `0`, but `float(' ')` raises.** Any Python-to-JS numeric
  parse needs an explicit blank check, or a blank field silently becomes zero.
- **A test comment claiming a fixture came from the other implementation is not
  evidence.** Regenerate it. The Twilio vectors were checked this way and were
  genuine, but the check is what made that a fact rather than an assumption.
```

## Style rules you must follow

These come from the repo's own rules and the document's existing voice.

- **British-neutral, plain prose. No marketing tone.** Never write "robust",
  "seamless", "comprehensive", "powerful", or "simply".
- **Do not add a summary paragraph under a heading** restating what the table
  below it already says (repo rule: `CLAUDE.md` → UI Copy, and this document
  follows it).
- Keep every existing Markdown table's column alignment style.
- Wrap prose at roughly 80 columns, as the rest of the file does.
- Do not renumber sections, do not reorder sections, and do not delete any
  section other than the Section 2 replacement specified above.
- Do not touch Sections 3, 4, 7, or 8 at all.

## When you are done

Print a short list of which of the seven edits you applied. If you could not
apply one, say which and why rather than inventing a substitute.
